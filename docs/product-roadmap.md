# Product Roadmap — Bandwire

> Generated by PLAID. Checkboxes are updated as tasks are completed.
> The coding agent MUST mark tasks `- [x]` as they are finished.

**Status:** 39/62 tasks complete
**Current Phase:** Phase 4 — Payments & Subscriptions

## Build Philosophy

1. **Each phase ships something usable.** No phase leaves the app broken. After any phase, you can demo what you've built.
2. **Infrastructure before features.** The first phase sets up everything so feature work in later phases is fast and clean.
3. **Magic moment first.** The core value proposition — venue posts date, gets matches, sends offer, band accepts, booking confirmed — works as early as possible.
4. **Test as you go.** Don't save testing for the end. Each task includes verification.
5. **Progressive enhancement.** Start with the simplest working version, then layer on polish.
6. **Review before proceeding.** Push each completed phase as a PR and let a review agent (e.g. CodeRabbit) review it before starting the next phase. External review catches issues the coding agent won't flag.
7. **Venue-first, then musician.** Build the paying customer's experience first, then the supply side. This mirrors the business model.
8. **Dark theme from day one.** Every component ships with the dark theme applied. No light mode → dark mode retrofit later.

---

## Phase 0: Foundation & Setup

> **Goal:** A running Next.js app with Convex backend, Clerk auth, Tailwind design system, and a working sign-up → role selection → dashboard shell flow. No features yet, but the scaffolding is solid.

**Reference sections — read these before starting this phase:**
- PRD: § Technical Architecture, § Auth Implementation, § Design System
- Vision: § Design Direction > Design Tokens, § Design Direction > Typography, § Design Direction > Color Palette

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 0. Then read only the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-0/foundation-and-setup`, commit, push, and open a PR for review."

- [x] **TASK-001** — Initialize Next.js project with TypeScript, Tailwind CSS, and App Router
  Files: `package.json`, `tsconfig.json`, `tailwind.config.ts`, `postcss.config.js`, `next.config.ts`
  Notes: Use `npx create-next-app@latest bandwire --typescript --tailwind --app --src-dir`. Verify: `npm run dev` starts and shows the default Next.js page at localhost:3000.

- [x] **TASK-002** — Configure Tailwind with Bandwire design tokens
  Files: `tailwind.config.ts`, `src/styles/globals.css`
  Notes: Add all color, font, spacing, border-radius, shadow, and max-width tokens from docs/prd.md § Design System > Tailwind Configuration. Set up CSS custom properties in globals.css. Import Inter and JetBrains Mono from Google Fonts. Set `html` and `body` to use `--color-background` and `--color-text`. Verify: Page background is dark (#0A0A0F) and text is light (#F1F1F3).

- [x] **TASK-003** — Set up Convex backend
  Files: `convex/`, `src/app/layout.tsx`, `.env.local`
  Notes: Run `npx convex dev` to initialize. Install `convex` package. Create `ConvexClientProvider` component. Add `NEXT_PUBLIC_CONVEX_URL` to `.env.local`. Verify: Convex dashboard shows the project, `npx convex dev` runs without errors.

- [x] **TASK-004** — Set up Clerk authentication
  Files: `src/app/layout.tsx`, `src/middleware.ts`, `.env.local`, `src/app/sign-in/[[...sign-in]]/page.tsx`, `src/app/sign-up/[[...sign-up]]/page.tsx`
  Notes: Install `@clerk/nextjs`. Create Clerk application in dashboard with email/password + Google OAuth. Configure the dark theme using `appearance={{ baseTheme: dark }}`. Set up `ClerkProvider` wrapping `ConvexProviderWithClerk`. Add middleware protecting `/dashboard/*` and `/onboarding/*` routes. Create sign-in and sign-up pages using Clerk's pre-built components. Set environment variables. Verify: Sign-up flow works end-to-end, user appears in Clerk dashboard.

- [x] **TASK-005** — Connect Clerk to Convex (JWT integration)
  Files: `convex/auth.config.ts`, `.env` (Convex env)
  Notes: In Clerk dashboard, create a JWT template named `convex`. In Convex, set `CLERK_JWT_ISSUER_DOMAIN` via `npx convex env set`. Configure `convex/auth.config.ts`. Verify: `ctx.auth.getUserIdentity()` returns the Clerk user in a test Convex function.

- [x] **TASK-006** — Create database schema
  Files: `convex/schema.ts`
  Notes: Implement the full schema from docs/prd.md § Data Model including all tables (users, venues, musicians, openDates, offers, bookings, notifications) with all fields, types, and indexes. Verify: `npx convex dev` processes the schema without errors.

- [x] **TASK-007** — Create auth helper functions
  Files: `convex/lib/auth.ts`
  Notes: Implement `requireUser`, `requireVenue`, `requireMusician` helpers as specified in docs/prd.md § Auth Implementation > Role-Based Access. These will be used by all subsequent Convex functions. Verify: Functions export correctly, TypeScript types are clean.

- [x] **TASK-008** — Create user management functions
  Files: `convex/users.ts`
  Notes: Implement `users.me` (query), `users.create` (mutation), `users.update` (mutation) as specified in docs/prd.md § API Specification > Users. The `create` mutation should validate username uniqueness and link to Clerk ID. Verify: Can create a user via Convex dashboard test runner.

- [x] **TASK-009** — Build UI primitives: Button, Input, Card, Badge, Modal, Toast, Skeleton
  Files: `src/components/ui/button.tsx`, `src/components/ui/input.tsx`, `src/components/ui/card.tsx`, `src/components/ui/badge.tsx`, `src/components/ui/modal.tsx`, `src/components/ui/toast.tsx`, `src/components/ui/skeleton.tsx`
  Notes: Build each component using the design token specs from docs/prd.md § Design System > Component Specifications. Use `clsx` and `tailwind-merge` for conditional class merging. Use `sonner` for toast implementation. All components should accept className prop for extensibility. Verify: Each component renders correctly in isolation with all variants (create a temporary /dev page to preview them).

- [x] **TASK-010** — Build dashboard layout shell with sidebar navigation
  Files: `src/app/dashboard/layout.tsx`, `src/components/layout/sidebar.tsx`, `src/components/layout/header.tsx`, `src/components/layout/mobile-nav.tsx`
  Notes: Dashboard layout: fixed sidebar (256px) on left, header at top, scrollable main content area. Sidebar shows navigation items — for now use placeholder links: Calendar, Matches, Bookings, Offers, Browse, Profile, Settings. Use Lucide icons for each nav item. Sidebar collapses to bottom nav on mobile (< 768px). Header shows Bandwire logo, notification bell placeholder, and Clerk `<UserButton />`. Apply all design tokens — dark background, surface sidebar, border dividers. Verify: Dashboard route renders with sidebar navigation, responsive on mobile.

- [x] **TASK-011** — Build onboarding flow: role selection page
  Files: `src/app/onboarding/page.tsx`
  Notes: After Clerk sign-up, user lands here. Two large clickable cards: "I'm a Venue/Promoter" (with building icon) and "I'm a Musician/Band" (with guitar icon). On selection, store choice in local state and navigate to the profile setup step. If user already has a profile (`users.me` returns data with `onboardingComplete: true`), redirect to `/dashboard`. Verify: New user sees role selection after sign-up, selecting a role navigates forward.

- [x] **TASK-012** — Build onboarding: venue profile setup form
  Files: `src/app/onboarding/page.tsx` (or a sub-component), `src/components/features/venue-profile-form.tsx`, `convex/venues.ts`
  Notes: Form fields: venue name (required), city (required), state (required), address (optional), capacity (required, number), genres (required, multi-select from predefined list), default deal type (required, select), default budget min/max (optional, number). Use `react-hook-form` with `zod` validation. On submit: call `users.create` (with role "venue") then `venues.create`, mark `onboardingComplete: true`, redirect to `/dashboard`. Genre list: use the 25-30 genres from docs/prd.md § Open Questions > OQ-1. Verify: Can complete venue onboarding end-to-end and land on dashboard.

- [x] **TASK-013** — Build onboarding: musician profile setup form
  Files: `src/app/onboarding/page.tsx` (or a sub-component), `src/components/features/musician-profile-form.tsx`, `convex/musicians.ts`
  Notes: Form fields: band name (required), genres (required, multi-select), city (required), state (required), bio (optional), music links — Spotify, Bandcamp, SoundCloud, YouTube, Apple Music (all optional URLs), social links (optional), monthly listeners (optional, number), avg draw (optional, number). Calculate `profileCompleteness` on create per formula in FR-008. On submit: call `users.create` (with role "musician") then `musicians.create`, mark `onboardingComplete: true`, redirect to `/dashboard`. Verify: Can complete musician onboarding end-to-end and land on dashboard.

- [x] **TASK-014** — Create constants file with genre list and deal types
  Files: `src/lib/constants.ts`
  Notes: Export `GENRES` array (25-30 genres), `DEAL_TYPES` array with labels and values (flat_fee, door_split, ticket_split, bar_split, other), `US_STATES` array for state selection. These will be reused across onboarding, open dates, and browse filters. Verify: Constants import correctly in onboarding forms.

- [x] **TASK-015** — Add onboarding middleware guard
  Files: `src/middleware.ts` or `src/app/dashboard/layout.tsx`
  Notes: In the dashboard layout, check if the user has completed onboarding (`users.me` returns user with `onboardingComplete: true`). If not, redirect to `/onboarding`. Also in onboarding, if already complete, redirect to `/dashboard`. This prevents users from skipping onboarding or returning to it unnecessarily. Verify: Unonboarded users can't access dashboard. Onboarded users skip onboarding.

---

## Phase 1: Core Booking Loop (Venue Side)

> **Goal:** Venues can post open dates, see matched musicians, and send booking offers. The venue side of the magic moment is working.

**Reference sections — read these before starting this phase:**
- PRD: § Data Model (openDates, offers), § API Specification (Open Dates, Matching, Offers), § User Stories (US-004, US-005, US-006), § Functional Requirements (FR-005, FR-006, FR-007), § UI/UX Requirements (Calendar, Matches, Offer Form)
- Vision: § Product Strategy > Core User Flows > Flow 1

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 1. Then read only the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-1/core-booking-venue-side`, commit, push, and open a PR for review."

- [x] **TASK-016** — Build open date Convex functions
  Files: `convex/openDates.ts`
  Notes: Implement `openDates.listByVenue` (query — returns all dates for the authenticated venue, ordered by date), `openDates.getWithMatches` (query — returns single open date with matched musicians), `openDates.create` (mutation — validates date is in the future, creates with status "open"), `openDates.update` (mutation — cannot update if status is "booked"), `openDates.cancel` (mutation — sets status to "cancelled"). All mutations use `requireVenue` auth helper. Verify: Can create and query open dates via Convex dashboard.

- [x] **TASK-017** — Build matching logic
  Files: `convex/matches.ts`
  Notes: Implement `matches.getForOpenDate` query. Matching algorithm: (1) Filter musicians where `isActive: true` and `profileCompleteness >= 60`. (2) Score each: genre tag intersection (count of overlapping genres / total requested genres) * 50 + same city bonus (30 if musician city matches venue city, 0 otherwise) + profileCompleteness * 0.2. (3) Sort by score descending. (4) Return top 20 matches with score and matchReasons array (e.g., ["Genre: indie rock, blues", "Same city"]). Verify: Create test musicians and an open date, confirm matching returns expected results.

- [x] **TASK-018** — Build calendar page
  Files: `src/app/dashboard/calendar/page.tsx`, `src/components/ui/calendar.tsx`
  Notes: Monthly calendar grid view. Each day cell is clickable. Days with open dates show a colored dot: blue for "open", amber for "hold", green for "booked". Show month/year header with prev/next navigation arrows. Use `date-fns` for date manipulation. On click of an empty day → open the "Create Open Date" modal. On click of an existing date → navigate to matches view for that date. Empty state for new venues: "Click any date to post your first open slot." Verify: Calendar renders current month, days are clickable, month navigation works.

- [x] **TASK-019** — Build open date creation form (modal)
  Files: `src/components/features/open-date-form.tsx`
  Notes: Modal form with fields: date (pre-filled from clicked calendar day, read-only), start time (optional), end time (optional), genres (multi-select from constants, pre-filled from venue defaults), deal type (select, pre-filled from venue default), budget min/max (number, pre-filled from venue defaults), notes (optional textarea). Use `react-hook-form` + `zod`. On submit: call `openDates.create`, close modal, show success toast "Open date posted!", calendar updates reactively. Verify: Can create an open date from the calendar, it appears with the blue "open" dot.

- [x] **TASK-020** — Build matches page
  Files: `src/app/dashboard/matches/page.tsx`, `src/components/features/match-card.tsx`
  Notes: Two-panel layout: left sidebar lists open dates (with status badges), right panel shows matches for the selected date. Match cards display: band name, genres (as badges), city, profile completeness bar, match score (as a number or visual), music link icons, and a "Send Offer" button. If no open dates exist: empty state directing to calendar. If an open date has no matches: "No matches yet. Try widening your genre criteria." Use skeleton loading states while matches load. Verify: Selecting an open date shows relevant matches. Match cards display correctly.

- [x] **TASK-021** — Build offer Convex functions (send, list sent)
  Files: `convex/offers.ts`
  Notes: Implement `offers.send` (mutation — creates offer, sets openDate status to "hold", creates notification for musician, checks free tier limit), `offers.listSent` (query — paginated list of offers sent by current venue user, includes musician band name). Use `requireVenue` auth helper. For free tier check: count offers sent this calendar month by this user, reject if >= 5 and user is on free tier. Verify: Can send an offer via Convex dashboard, open date status changes to "hold".

- [x] **TASK-022** — Build offer form modal
  Files: `src/components/features/offer-form.tsx`
  Notes: Modal triggered from "Send Offer" button on match cards. Pre-fills from the open date: date, start/end time, deal type, budget. Editable fields: all of the above plus load-in time, set length (minutes), notes. On submit: call `offers.send`, close modal, show toast "Offer sent!". If free tier limit reached: replace form with upgrade prompt showing "You've used all 5 free offers this month. Upgrade to Pro for unlimited booking." with a button linking to settings. Verify: Can send an offer from the match card, toast appears, offer status is "pending".

- [x] **TASK-023** — Build sent offers list page (venue view)
  Files: `src/app/dashboard/offers/page.tsx`, `src/components/features/offer-card.tsx`
  Notes: List of sent offers with tabs: All, Pending, Accepted, Declined, Countered. Each offer card shows: musician band name, date, deal type + amount, status badge (color-coded: pending=blue, accepted=green, declined=red, countered=amber). Click card → navigate to offer detail view (build in Phase 2). Paginated with Convex cursor pagination. Empty state: "No offers sent yet. Head to your calendar to post an open date and find matches." Verify: Sent offers appear after sending from match page.

- [x] **TASK-024** — Build venue dashboard home
  Files: `src/app/dashboard/page.tsx`
  Notes: Role-dependent dashboard. For venue users: three summary cards at top (upcoming open dates count, pending offers count, confirmed bookings count — use placeholder 0 for bookings until Phase 2). Below: list of next 5 upcoming open dates with status badges. Each card links to the relevant page. Empty state for new venues: "Welcome to Bandwire! Start by posting an open date on your calendar." with CTA button to calendar. Verify: Dashboard shows correct counts and upcoming dates for venue users.

---

## Phase 2: Core Booking Loop (Musician Side)

> **Goal:** Musicians can view and respond to offers (accept/decline/counter). Accepting an offer creates a booking with an auto-generated contract. The full magic moment is working end-to-end.

**Reference sections — read these before starting this phase:**
- PRD: § API Specification (Offers — accept/decline/counter, Bookings), § User Stories (US-007, US-008, US-009, US-010), § Functional Requirements (FR-009, FR-010, FR-011), § UI/UX Requirements (Offers, Booking Detail)
- Vision: § Product Strategy > Core User Flows > Flow 2, § Product Strategy > Magic Moment Design

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 2. Then read only the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-2/core-booking-musician-side`, commit, push, and open a PR for review."

- [x] **TASK-025** — Build offer response Convex functions
  Files: `convex/offers.ts`
  Notes: Add to existing offers.ts: `offers.listReceived` (query — paginated list of offers received by current musician, includes venue name), `offers.getById` (query — full offer details with venue and musician info, auth: sender or recipient only), `offers.accept` (mutation — creates booking, sets openDate to "booked", notifies venue), `offers.decline` (mutation — optional reason, sets openDate back to "open", notifies venue), `offers.counter` (mutation — stores counter-offer terms, notifies venue), `offers.acceptCounter` (mutation — venue accepts counter, creates booking same as accept), `offers.withdraw` (mutation — venue withdraws, sets openDate to "open"). Verify: Each mutation works correctly via Convex dashboard.

- [x] **TASK-026** — Build booking and contract Convex functions
  Files: `convex/bookings.ts`, `convex/contracts.ts`
  Notes: `bookings.listMine` (query — returns bookings where user is venue or musician side, paginated), `bookings.getById` (query — full booking with venue + musician details, auth: either party), `bookings.cancel` (mutation — either party, sets status "cancelled", reverts open date to "open", notifies other party), `bookings.acknowledgeContract` (mutation — sets the caller's acknowledgment flag to true). In `contracts.ts`: create a helper function `generateContractText(booking, venue, musician)` that returns a formatted contract string with: date, time, venue name + address, artist name, deal terms, cancellation policy ("Either party may cancel with 48 hours notice"), and disclaimer. Verify: Accepting an offer creates a booking with generated contract text.

- [x] **TASK-027** — Build notification Convex functions
  Files: `convex/notifications.ts`
  Notes: Implement `notifications.list` (query — paginated, ordered by createdAt desc), `notifications.unreadCount` (query — count of unread for current user), `notifications.markRead` (mutation), `notifications.markAllRead` (mutation). Also create a helper `createNotification(ctx, { userId, type, title, message, linkUrl, relatedOfferId?, relatedBookingId? })` used internally by offer and booking mutations. Verify: Creating an offer generates a notification for the musician. Unread count reflects correctly.

- [x] **TASK-028** — Build received offers page (musician view)
  Files: `src/app/dashboard/offers/page.tsx` (extend existing)
  Notes: The offers page should detect user role and show different views. Musician view: tabs for Pending, Accepted, Declined. Each offer card shows: venue name, date, deal type + amount, status badge. Pending offers have Accept/Decline/Counter action buttons directly on the card. Click card → offer detail view. Verify: Musician user sees received offers with action buttons.

- [x] **TASK-029** — Build offer detail page
  Files: `src/app/dashboard/offers/[offerId]/page.tsx`
  Notes: Full-page view of a single offer. Shows: venue profile summary (name, location, capacity, photo), offer terms (date, time, deal type, amount, notes), and action area. For musicians with pending offers: Accept, Decline, Counter buttons. Decline shows a confirmation modal with optional reason input. Counter opens inline form to modify amount/split/deal type with a notes field. For venues viewing countered offers: shows original terms vs counter-proposal, with Accept Counter and Decline buttons. Status history shown as a timeline if multiple interactions. Verify: Full offer flow works: venue sends → musician sees → musician accepts → booking created.

- [x] **TASK-030** — Build booking list page
  Files: `src/app/dashboard/bookings/page.tsx`, `src/components/features/booking-card.tsx`
  Notes: List of all bookings for the current user (venue or musician). Tabs: Upcoming (confirmed, date >= today), Past (completed), Cancelled. Booking cards show: band/venue name, date, deal type + amount, status badge. Click → booking detail page. Empty state: "No bookings yet. Your confirmed gigs will appear here." Verify: After accepting an offer, booking appears in this list.

- [x] **TASK-031** — Build booking detail page with contract
  Files: `src/app/dashboard/bookings/[bookingId]/page.tsx`, `src/components/features/contract-view.tsx`
  Notes: Full booking details: both parties' info (venue name + details, band name + details), date, time, deal terms. Below: contract section rendered as a styled document (use Card component with slightly different styling — wider padding, serif-like heading treatment for the contract title). Contract shows the generated text from `generateContractText`. "Acknowledge Contract" button if the current user hasn't acknowledged yet. Both acknowledgments shown as checkmarks. "Cancel Booking" button opens confirmation modal with reason input. Verify: Booking detail shows all info, contract text renders, acknowledgment flow works.

- [x] **TASK-032** — Build notification bell and dropdown
  Files: `src/components/layout/header.tsx`, `src/components/features/notification-list.tsx`
  Notes: Notification bell icon in header with unread count badge (red circle with number). On click: dropdown panel showing recent notifications (last 10). Each notification shows: icon by type, title, message preview, time ago (use date-fns `formatDistanceToNow`). Click notification → navigate to linkUrl, mark as read. "Mark all as read" link at top of dropdown. Unread count updates in real-time via Convex reactive query. Verify: Sending an offer triggers notification for musician, bell badge updates, clicking navigates to offer.

- [x] **TASK-033** — Build musician dashboard home
  Files: `src/app/dashboard/page.tsx` (extend existing)
  Notes: Extend dashboard page to handle musician role. Summary cards: pending offers count, confirmed bookings count, profile completeness percentage. Below: list of recent pending offers (up to 5). If profile completeness < 60: prominent banner "Complete your profile to appear in venue matches" with link to profile edit. Empty state: "You're all set! When venues send offers, they'll appear here." Verify: Musician dashboard shows correct counts and profile completeness.

- [x] **TASK-034** — Wire up sidebar navigation with role-based items
  Files: `src/components/layout/sidebar.tsx`
  Notes: Update sidebar to show role-appropriate items. Venue: Calendar, Matches, Bookings, Offers, Browse, Profile, Settings. Musician: Bookings, Offers, Browse, Profile, Settings. Active route highlighted with primary color. Notification count badge on the main header bell (already done in TASK-032). Verify: Sidebar shows correct items per role, active state works.

---

## Phase 3: Browse, Discovery & Profiles

> **Goal:** Venues can browse musicians beyond matches, musicians can browse venues, and public profiles are viewable. The discovery side of the marketplace is working.

**Reference sections — read these before starting this phase:**
- PRD: § API Specification (Musicians — browse, Venues — browse), § Functional Requirements (FR-004, FR-008, FR-012, FR-013), § UI/UX Requirements (Browse, Profile Edit, Public Profile)
- Vision: § Product Strategy > Core User Flows > Flow 3

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 3. Then read only the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-3/browse-discovery-profiles`, commit, push, and open a PR for review."

- [x] **TASK-035** — Build browse Convex functions
  Files: `convex/musicians.ts`, `convex/venues.ts`
  Notes: Add `musicians.browse` query: paginated, filterable by city and genres, sortable by profileCompleteness or createdAt. Only return `isActive: true` musicians. Add `venues.browse` query: paginated, filterable by city and genres, only `isActive: true`. Both use Convex cursor pagination. Verify: Browse queries return correct filtered results.

- [x] **TASK-036** — Build browse page
  Files: `src/app/dashboard/browse/page.tsx`
  Notes: Role-dependent view. Filter bar at top: genre multi-select, city text input. Below: paginated grid of profile cards (3 columns desktop, 2 tablet, 1 mobile). Venue users see musician cards (with "Send Offer" button that opens a modal to select which open date to attach). Musician users see venue cards (view-only). Cards show: name, photo (or placeholder), genres as badges, city, and profile completeness (musicians only). "Load more" button for pagination. Empty results: "No results found. Try adjusting your filters." Verify: Browse page shows filtered results for both roles.

- [x] **TASK-037** — Build profile edit page
  Files: `src/app/dashboard/profile/page.tsx`
  Notes: Reuse the venue-profile-form and musician-profile-form components from onboarding, but pre-filled with existing data. Add photo upload functionality: click to upload or drag-and-drop, files go to Convex file storage, store returned URLs in the profile. Allow up to 5 photos. Show profile completeness for musicians with suggestions for missing fields. "Save Changes" button at bottom, success toast on save. Verify: Can update profile fields, photos upload and display correctly.

- [x] **TASK-038** — Build public profile page
  Files: `src/app/[username]/page.tsx`
  Notes: Publicly accessible (no auth required). Fetch user by username, then fetch venue or musician profile based on role. Musician profile: large header with band name and photo, bio, genre badges, location, music links as clickable icons (Spotify, Bandcamp, etc.), social links, stats (monthly listeners, avg draw if provided). Venue profile: venue name, photo, description, location, capacity, genres, website/social links. Use a different layout than the dashboard — full-width, centered content, no sidebar. 404 if username not found. Verify: Navigate to /[username] shows the public profile without authentication.

- [x] **TASK-039** — Add photo upload component
  Files: `src/components/ui/photo-upload.tsx`, `convex/files.ts`
  Notes: Reusable photo upload component. Click or drag-and-drop. Accept jpg, png, webp only, max 5MB. Upload to Convex file storage via `ctx.storage.store()`. Return the storage URL. Show upload progress indicator. Display uploaded photos as a small gallery with remove button on hover. Used in both venue and musician profile forms. Verify: Can upload a photo, it displays in the form, and persists after saving.

---

## Phase 4: Payments & Subscription Gating

> **Goal:** Venue owners can upgrade to Pro via Polar checkout. Free tier enforces the 5 offers/month limit. Subscription status is displayed in settings.

**Reference sections — read these before starting this phase:**
- PRD: § Payment Integration, § Functional Requirements (FR-016, FR-017), § UI/UX Requirements (Settings)
- Vision: § Product Strategy > MVP Definition (subscription gating)

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 4. Then read only the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-4/payments-and-subscriptions`, commit, push, and open a PR for review."

- [ ] **TASK-040** — Set up Polar product and webhook endpoint
  Files: `convex/http.ts`, `convex/subscriptions.ts`
  Notes: Create "Bandwire Pro" product in Polar dashboard with monthly price ($29/month for launch). Implement Convex HTTP endpoint at `/webhooks/polar` that validates webhook signature and handles events: `subscription.created` (update user to "pro"), `subscription.updated`, `subscription.canceled` (update user to "free"). Create `subscriptions.getStatus` query that returns current tier, offers this month, offer limit, and `canSendOffer` boolean. Create `subscriptions.createCheckout` mutation that generates a Polar checkout URL. Verify: Webhook endpoint responds to test events from Polar.

- [ ] **TASK-041** — Enforce free tier offer limit
  Files: `convex/offers.ts`
  Notes: Update `offers.send` mutation to check subscription status before allowing the offer. If `canSendOffer` is false, throw ConvexError with message "Monthly offer limit reached. Upgrade to Pro for unlimited offers." Update the offer form modal to check status before rendering the form — if limit reached, show upgrade prompt instead of form. Verify: 6th offer attempt on free tier shows upgrade prompt.

- [ ] **TASK-042** — Build settings page with subscription management
  Files: `src/app/dashboard/settings/page.tsx`
  Notes: Three sections: Account Info (name, email — read-only from Clerk, with link to Clerk's user profile for edits), Subscription Status (current tier badge, offers used this month / limit, upgrade button for free tier or "Manage Subscription" link to Polar billing portal for pro tier), Notification Preferences (placeholder toggles for email notifications — actual email implementation in Phase 5). Upgrade button calls `subscriptions.createCheckout` and redirects to the returned Polar URL. Verify: Settings page shows correct tier, upgrade flow redirects to Polar checkout.

- [ ] **TASK-043** — Add subscription status indicator to dashboard
  Files: `src/app/dashboard/page.tsx`, `src/components/layout/sidebar.tsx`
  Notes: For venue users: show a small badge in the sidebar footer indicating current tier ("Free" or "Pro"). On the dashboard home, if free tier and offers used > 3, show a subtle banner: "You've used X of 5 free offers this month." with upgrade link. Verify: Free tier users see usage indicator, pro users see pro badge.

---

## Phase 5: Email Notifications

> **Goal:** Users receive email notifications for key booking events. Emails are branded, concise, and link back to Bandwire.

**Reference sections — read these before starting this phase:**
- PRD: § Functional Requirements (FR-015), § Dependencies & Integrations (Resend)
- Vision: § Brand Strategy > Voice & Tone Guide

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 5. Then read only the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-5/email-notifications`, commit, push, and open a PR for review."

- [ ] **TASK-044** — Set up Resend and create email templates
  Files: `convex/lib/email.ts`
  Notes: Install Resend SDK (use Node.js `resend` package). Create a `sendEmail` helper function that accepts `to`, `subject`, and `html` params. Create HTML email template function that wraps content in a branded layout: dark background (#0A0A0F), white text, Bandwire logo at top, primary action button in electric blue (#3B82F6), minimal footer with unsubscribe placeholder. Template should be inline-styled (email clients don't support CSS classes). Set `RESEND_API_KEY` in Convex env. Verify: Can send a test email that renders correctly.

- [ ] **TASK-045** — Wire up email notifications to booking events
  Files: `convex/offers.ts`, `convex/bookings.ts`
  Notes: Add email sends to existing mutations using Convex's `ctx.scheduler.runAfter(0, ...)` to send asynchronously without blocking the mutation. Events that trigger email: new offer received (to musician), offer accepted (to venue), offer declined (to venue), offer countered (to venue), counter accepted (to musician), booking confirmed (to both), booking cancelled (to other party). Each email: short subject line, 2-3 sentence body matching the brand voice ("You're locked in!" not "Your booking has been confirmed"), and a CTA button linking to the relevant page in Bandwire. Verify: Accepting an offer sends email to both parties with correct content and links.

- [ ] **TASK-046** — Add email preference toggles to settings
  Files: `src/app/dashboard/settings/page.tsx`, `convex/users.ts`
  Notes: Add `emailPreferences` field to the users table (object with boolean flags for each notification type). Update settings page to show toggles: "New offers" (musician), "Offer responses" (venue), "Booking confirmations" (both), "Booking cancellations" (both). Default all to true. Update email send logic to check preferences before sending. Verify: Toggling off a preference prevents that email type from being sent.

---

## Phase 6: Polish & Launch Prep

> **Goal:** Comprehensive error handling, loading states, empty states, landing page, responsive design pass, and accessibility review. The app is ready for real users.

**Reference sections — read these before starting this phase:**
- PRD: § Non-Functional Requirements, § Edge Cases & Error Handling, § UI/UX Requirements (Landing Page)
- Vision: § Design Direction > Accessibility Commitments, § Design Direction > Motion & Interaction, § Brand Strategy > Voice & Tone Guide
- GTM: § Launch Strategy, § Pre-Launch Playbook

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 6. Then read only the Reference sections listed above from docs/prd.md, docs/product-vision.md, and docs/gtm.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-6/polish-and-launch-prep`, commit, push, and open a PR for review."

- [ ] **TASK-047** — Build landing page
  Files: `src/app/page.tsx`
  Notes: Marketing landing page. Sections: (1) Hero — headline "The booking platform built for the grassroots music scene." with subheadline and "Get Started" CTA. (2) Value props — three columns: for venues, for musicians, for promoters, each with icon and short description. (3) How it works — 3-step visual (Post → Match → Book). (4) Social proof placeholder — "Join X venues and X musicians" (pull real counts from Convex). (5) Final CTA — "Ready to plug in?" with sign-up button. Dark background, electric blue accents, high-contrast. Respect prefers-reduced-motion for any scroll animations. Verify: Landing page renders beautifully, all CTAs link to sign-up, mobile responsive.

- [ ] **TASK-048** — Comprehensive empty states
  Files: All page components
  Notes: Review every page and ensure empty states exist and are helpful (not just "No data"). Calendar: "Click any date to post your first open slot." Matches: "No matches yet. Post an open date to get started." Offers (venue): "No offers sent yet. Find your next act from your matches." Offers (musician): "No offers yet. Make sure your profile is complete so venues can find you." Bookings: "No bookings yet. Your confirmed gigs will appear here." Browse (no results): "No results found. Try adjusting your filters." Use illustrations or icons to make empty states feel intentional, not broken. Verify: Each page has a well-designed empty state.

- [ ] **TASK-049** — Loading states and skeletons
  Files: All page components
  Notes: Every page that fetches data should show skeleton loading states instead of blank screens or spinners. Use the Skeleton component from TASK-009. Match the layout of the actual content — if a page shows 3 cards, show 3 skeleton cards. Calendar: skeleton grid. Offer cards: skeleton cards. Dashboard summary: skeleton cards. List pages: 3-5 skeleton rows. Verify: Throttle network in dev tools, confirm all pages show skeletons before data loads.

- [ ] **TASK-050** — Error handling and error boundaries
  Files: `src/app/error.tsx`, `src/app/not-found.tsx`, `src/components/ui/error-boundary.tsx`
  Notes: Global error page (`error.tsx`): "Something went wrong" with retry button. 404 page (`not-found.tsx`): "Page not found — let's get you back on track" with link to dashboard. Add error boundaries around feature components so a failure in one section doesn't crash the whole page. Toast notifications for mutation errors (ConvexError messages displayed in toast). Network offline detection: if navigator is offline, show a banner at top of page "You're offline. Changes will sync when you reconnect." Verify: Navigating to invalid routes shows 404. Convex errors show as toasts.

- [ ] **TASK-051** — Form validation pass
  Files: All form components
  Notes: Review all forms (onboarding, open date, offer, counter-offer, profile edit) for comprehensive validation. Ensure: required fields show errors on empty submit, numbers are positive, URLs are valid format, date fields reject past dates, character limits on text fields (bio: 1000 chars, notes: 500 chars). All validation uses zod schemas with react-hook-form. Inline error messages below invalid fields. Verify: Submit each form with invalid data, confirm all errors display correctly.

- [ ] **TASK-052** — Responsive design pass
  Files: All page and component files
  Notes: Test every page at mobile (375px), tablet (768px), and desktop (1280px) breakpoints. Key areas: sidebar collapses to bottom nav on mobile, calendar adapts to smaller cells on mobile, match cards stack single-column on mobile, forms are full-width on mobile, modals are full-screen on mobile (< 640px). Ensure no horizontal scroll on any breakpoint. Touch targets minimum 44x44px on mobile. Verify: Every page works at all three breakpoints without layout breaks.

- [ ] **TASK-053** — Accessibility pass
  Files: All components
  Notes: Run through WCAG 2.1 AA checklist: all interactive elements keyboard navigable (Tab through the entire app), visible focus indicators (2px ring, primary color), form inputs have labels (not just placeholders), images have alt text, color contrast meets 4.5:1 for text, ARIA labels on icon-only buttons, `role="alert"` on toast notifications, skip navigation link at top of page. Add `prefers-reduced-motion` media query to disable animations. Verify: Navigate entire app using only keyboard. Run Lighthouse accessibility audit, target 90+ score.

- [ ] **TASK-054** — Performance optimization
  Files: Various
  Notes: Run Lighthouse performance audit. Target: LCP < 2s, bundle size < 250KB. Optimize: ensure Next.js image optimization for photos, lazy-load below-fold content on landing page, code-split dashboard pages (Next.js does this automatically with App Router). Convex queries should use indexes (already set up in schema). Review and remove any unnecessary re-renders in React components. Verify: Lighthouse performance score > 80.

- [ ] **TASK-055** — SEO and metadata
  Files: `src/app/layout.tsx`, `src/app/page.tsx`, `src/app/[username]/page.tsx`
  Notes: Add metadata to root layout: title "Bandwire — Book Live Music in Minutes", description using the one-liner. Add Open Graph tags for social sharing. Public profile pages should have dynamic meta titles ("[Band Name] on Bandwire" or "[Venue Name] on Bandwire"). Add a basic `robots.txt` and `sitemap.xml` (can be static for MVP). Verify: Share a Bandwire link on social media, confirm preview card shows correctly.

---

## Phase 7: Post-Launch Iteration

> **Goal:** Gather user feedback, implement high-demand P2 features, and optimize based on real usage data. This phase evolves based on actual user needs.

**Reference sections — read these before starting this phase:**
- PRD: § Out of Scope (review for features to pull forward), § Open Questions
- Vision: § Product Strategy > Feature Priority (MoSCoW) — "Could Have" items
- GTM: § Post-Launch Growth

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 7. Then read only the Reference sections listed above from docs/prd.md, docs/product-vision.md, and docs/gtm.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-7/post-launch-iteration`, commit, push, and open a PR for review."

- [ ] **TASK-056** — Add musician availability calendar
  Files: `src/app/dashboard/calendar/page.tsx` (extend for musician role), `convex/musicians.ts`
  Notes: Musicians should be able to mark dates as available or unavailable on a calendar. Add an `availability` table or field to the musician profile. The matching algorithm should factor in availability — only match musicians who are available on the open date. Calendar page for musicians: shows their bookings (confirmed) and availability markers. Verify: Musician sets availability, matching respects it.

- [ ] **TASK-057** — Add venue deal templates
  Files: `src/components/features/deal-template.tsx`, `convex/venues.ts`
  Notes: Venues can save and reuse common deal configurations (e.g., "Friday Night Standard": flat fee, $400, 9pm-11pm, 90min set). Add a `dealTemplates` array field to the venue profile. When creating an open date or sending an offer, venues can select from saved templates to auto-fill. Verify: Create a template, use it when posting an open date, fields auto-fill.

- [ ] **TASK-058** — Add offer expiration
  Files: `convex/offers.ts`, `convex/crons.ts`
  Notes: Offers should auto-expire after 7 days of no response. Add a Convex cron job that runs daily, finds pending offers older than 7 days, marks them as "expired", notifies the venue, and reverts the open date to "open". Verify: Create an offer, wait for expiration (or manually adjust createdAt for testing), confirm it expires and notifications fire.

- [ ] **TASK-059** — Add Google Calendar export
  Files: `src/lib/calendar-export.ts`
  Notes: Add "Add to Google Calendar" button on confirmed bookings. Generate a Google Calendar link with the booking details (date, time, venue/band name, location). Opens in a new tab. Simple URL construction, no API integration needed. Verify: Clicking the link opens Google Calendar with pre-filled event details.

- [ ] **TASK-060** — Add basic analytics dashboard for venues
  Files: `src/app/dashboard/analytics/page.tsx`, `convex/analytics.ts`
  Notes: Simple analytics page for venue users showing: total bookings this month vs last month, offers sent vs accepted (conversion rate), most booked genres (pie chart), upcoming schedule density. Use data already in the database — no separate analytics tracking needed. Keep it simple — 3-4 cards with key metrics. Verify: Analytics page shows accurate data based on the venue's booking history.

- [ ] **TASK-061** — Add onboarding tutorial/walkthrough
  Files: `src/components/features/onboarding-tour.tsx`
  Notes: After first sign-up and profile creation, show a brief guided tour of the dashboard (3-4 steps). Step 1: "This is your calendar — post open dates here." Step 2: "Matches will appear here when you post a date." Step 3: "Track your offers and bookings here." Use a simple tooltip/popover approach pointing to sidebar items. Dismissible, only shows once (track in user record). Verify: New user sees tour after onboarding, tour doesn't reappear after dismissal.

- [ ] **TASK-062** — Collect and respond to user feedback
  Files: `src/components/features/feedback-widget.tsx`
  Notes: Add a small feedback button in the dashboard footer ("Send Feedback"). Opens a simple modal with a textarea and submit button. Sends feedback to a Convex table or directly to Jon's email via Resend. Include user's role and email for context. This is critical for the post-launch iteration loop. Verify: Feedback submission works and arrives at the designated destination.

---

## Agent Session Guide

### How to Use This Roadmap with Your Coding Agent

1. **Start a session:** Give your coding agent the phase prompt at the beginning of each phase.
2. **Read selectively:** Each phase lists its Reference sections — the specific parts of the PRD, vision doc, and GTM doc needed for that phase. The agent should read only those sections, not the entire documents.
3. **Let it work:** The agent reads the roadmap, finds the first unchecked task, implements it, and marks it complete.
4. **One session = one phase (ideally):** Try to complete a full phase in one session for best continuity. If you need to stop, the agent can resume from the last unchecked task.
5. **Push a PR for review:** When a phase is complete, push the work as a PR and let a review agent (e.g. [CodeRabbit](https://coderabbit.ai)) review it before starting the next phase. See the Phase Review section below.
6. **Need more context?** If a task references a section not in the phase's Reference sections, the agent should read just that section on demand.

### Session Tips

- **Don't read everything:** The PRD, vision doc, and GTM doc can be large. Each phase's Reference sections tell the agent exactly what to read. Loading the full documents wastes context.
- **Don't skip tasks:** Tasks are ordered intentionally. Skipping creates dependency issues.
- **Verify after each phase:** Run the app after completing a phase to confirm everything works before moving on.
- **Review before moving on:** Push a PR for each completed phase and let your review agent check it. Don't start the next phase until the PR is merged. This catches issues early when they're cheap to fix.
- **Update the status line:** After completing tasks, update the header status: `**Status:** X/62 tasks complete` and `**Current Phase:** Phase N`.

### Prompt Templates

**Starting a new phase:**
> "Read docs/product-roadmap.md and find the current phase. Read only the Reference sections listed for that phase from docs/prd.md, docs/product-vision.md, and docs/gtm.md. Start working on the first unchecked task. After completing each task, update the checkbox to [x] in the roadmap file. Continue through the phase."

**Resuming after a break:**
> "Read docs/product-roadmap.md. Find where we left off (first unchecked task). Read only the Reference sections listed for the current phase from docs/prd.md, docs/product-vision.md, and docs/gtm.md. Continue from the first unchecked task."

**After completing a phase:**
> "Phase [N] is complete. Create a branch called phase-{N}/{slug}, commit all work, push, and open a PR targeting main. Title it 'Phase {N}: {Title}' and include the phase goal and completed task count in the body."

**Fixing an issue:**
> "There's a problem with [description]. Read the relevant section of docs/prd.md for the expected behavior and fix it. Don't mark any new tasks complete until the fix is verified."

### Phase Review Workflow

After completing every phase:

1. **Branch:** Create `phase-{N}/{phase-slug}` (e.g. `phase-0/foundation-and-setup`)
2. **Commit & push:** Stage all work, commit with "Phase {N}: {Phase Title}", push to origin
3. **Open PR:** Target main branch, include phase goal and task count in the PR body
4. **Review:** Let your review agent (recommend [CodeRabbit](https://coderabbit.ai) — free for open-source) review the PR
5. **Address feedback:** Fix any issues flagged by the reviewer
6. **Merge:** Once clean, merge and start the next phase
